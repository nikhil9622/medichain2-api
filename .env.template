# Copy this file to `.env` and fill the secret values. NEVER commit your real
# keys to source control. Use Vercel or your deployment platform to store
# production secrets.

# Supabase project URL (public, safe to share)
SUPABASE_URL=https://ddobqbscfjqlqcdinsko.supabase.co

# Supabase keys
# - SUPABASE_ANON_KEY : public anon key for browser clients (safe only if RLS is enforced)
# - SUPABASE_SERVICE_ROLE_KEY : server-side admin key (keep secret)
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Database URL for server-side Postgres connection (used by SQLModel)
# Example: postgresql://<user>:<pass>@db.<region>.supabase.co:5432/postgres?sslmode=require
SUPABASE_DB_URL=

# Optional: override sqlite DB file locally (not used in production)
DB_FILE=medichain2.db
